server:
  port: 8080

spring:
  datasource:
    username: root
    password: xxw+2019
    url: jdbc:mysql://localhost:3306/asset_manage?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.jdbc.Driver

mybatis:
  config-location: classpath:config/mybatis/mybatis-config.xml
  mapperLocations: classpath:mybatis/mapper/*Mapper.xml
  typeAliasesPackage: com.tusdao.**.model
  checkConfigLocation: true

#showSql
logging:
  level:
    com:
      example:
        mapper : debug
